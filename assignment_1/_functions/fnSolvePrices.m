function [w, T] = fnSolvePrices()
    % Initialise
    w_temp              = 1.0;
    T_temp              = 1.0;
    iter                = 0;
    conv                = false;

    % Start the process
    while iter < pMaxIter
        
    % End the loop
    end
    
    % Save the results

end