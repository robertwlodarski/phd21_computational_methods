function [RESULT]       = fnSimulateAiyagari1994(Parameters,Grids)

%% 1. Unpacking
% Parameters

% Grids
vGridA1                 = Grids.vGridA1;
vGridZ                  = Grids.vGridZ;


%% 2. Set-up
% Iterations business
iWeightOld              = 0.9;
% Value function + asset & consumption policies
iValue                  = repmat(0.1*vGridA1,1,size(vGridZ,1));
iValueNew               = zeros(size(iValue));
iPolicyC                = iValueNew;



end